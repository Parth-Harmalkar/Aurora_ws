---
phase: 4
plan: 2
wave: 2
---

# Plan 4.2: OAK-D Spatial Neural Network Integration

## Objective
Activate the on-device VPU Neural Network on the OAK-D Lite to publish 3D spatial bounding boxes, and ingest these `vision_msgs` directly into `ai_bridge_node` to give the LangGraph LLM continuous semantic vision.

## Context
- .gsd/SPEC.md
- .gsd/ROADMAP.md
- /home/aurora/aurora_ws/ros2_ws/src/aurora_bringup/launch/camera.launch.py
- /home/aurora/aurora_ws/ros2_ws/src/aurora_ai_bridge/aurora_ai_bridge/ai_bridge_node.py

## Tasks

<task type="auto">
  <name>Enable OAK-D Spatial Detection Node</name>
  <files>
    - /home/aurora/aurora_ws/ros2_ws/src/aurora_bringup/launch/camera.launch.py
  </files>
  <action>
    - Modify `camera.launch.py` to enable the neural network module from `depthai_ros_driver`.
    - Configure the `camera_model` parameters to output standard `vision_msgs/Detection3DArray` or compatible spatial class bounding boxes.
  </action>
  <verify>ros2 launch aurora_bringup camera.launch.py and check `ros2 topic list` for neural network outputs</verify>
  <done>The camera publishes live 3D bounding boxes to the ROS network without utilizing the Jetson CPU.</done>
</task>

<task type="auto">
  <name>Bridge Vision into LangGraph Perception</name>
  <files>
    - /home/aurora/aurora_ws/ros2_ws/src/aurora_ai_bridge/aurora_ai_bridge/ai_bridge_node.py
    - /home/aurora/aurora_ws/ros2_ws/src/aurora_ai_bridge/package.xml
  </files>
  <action>
    - Add `vision_msgs` or `depthai_ros_msgs` to `package.xml` dependencies.
    - Add a subscriber to the OAK-D spatial detections topic in `ai_bridge_node.py`.
    - Create a callback that parses the highest-confidence detections (e.g., "Person at 2.5m") and updates `self.sensor_state["vision"]`.
    - Ensure empty detections gracefully fallback to "Vision Clear".
  </action>
  <verify>Check compilation with `colcon build --packages-select aurora_ai_bridge`</verify>
  <done>The AI log dynamically registers object classifications instead of remaining "Offline".</done>
</task>

## Success Criteria
- [ ] OAK-D VPU correctly executes spatial object detection.
- [ ] `ai_bridge_node` subscribes and safely parses the detection arrays.
- [ ] LangGraph reasoner automatically ingests semantic object strings into its situational context.
