---
phase: 4
plan: 2
wave: 2
---

# Plan 4.2: Label Mapping & Metadata Filtering

## Objective
Enhance the object detection data by adding human-readable labels and providing a confidence filtering mechanism to reduce noise.

## Context
- .gsd/SPEC.md
- [camera_node.py](file:///home/aurora/aurora_ws/ros2_ws/src/aurora_camera/aurora_camera/camera_node.py)
- [RESEARCH.md](file:///home/aurora/aurora_ws/.gsd/phases/4/RESEARCH.md)

## Tasks

<task type="auto">
  <name>Map Label IDs to Strings</name>
  <files>ros2_ws/src/aurora_camera/aurora_camera/camera_node.py</files>
  <action>
    Add a label map (dictionary) for MobileNet-SSD (PASCAL VOC classes: person, bottle, chair, etc.):
    1. Map numeric IDs from DepthAI to strings.
    2. Populate `hypothesis.label` field in `vision_msgs/Detection3D`.
    3. Ensure class-agnostic filtering if desired.
  </action>
  <verify>`ros2 topic echo /camera/detections` includes class names in Hypothesis.</verify>
  <done>Detections are human-readable.</done>
</task>

<task type="auto">
  <name>Confidence Filtering & Parametrization</name>
  <files>ros2_ws/src/aurora_camera/aurora_camera/camera_node.py</files>
  <action>
    Introduce a confidence threshold (default: 0.5):
    1. Discard detections below the threshold on-device if possible, or in the node.
    2. Add standard ROS 2 parameters for `confidence_threshold` and `spatial_calc_config` (to tune the depth ROI).
  </action>
  <verify>Increasing `confidence_threshold` via ROS 2 CLI suppresses noisy detections.</verify>
  <done>Detection quality is tunable without rebuilding.</done>
</task>

## Success Criteria
- [ ] Detections include labeled strings in `vision_msgs`.
- [ ] Noisy detections (confidence < 0.5) are silenced.
- [ ] ROS 2 parameters control detection behavior at runtime.
